jdk:
  - openjdk17
before_install:
  - sdk install java 25.0.3-zulu || sdk install java 25-zulu || sdk install java 25-open || sdk install java 25.0.3-tem || true
  - sdk use java 25.0.3-zulu || sdk use java 25-zulu || sdk use java 25-open || sdk use java 25.0.3-tem || true
