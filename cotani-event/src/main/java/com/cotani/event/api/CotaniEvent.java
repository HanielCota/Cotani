package com.cotani.event.api;

/**
 * Marker interface for all Cotani domain events.
 *
 * <p>This interface intentionally has no Bukkit dependency. Each module should
 * define its own events by implementing this contract.</p>
 */
public interface CotaniEvent {}
